services:
  app:
    build: .
    container_name: note-api
    restart: always
    env_file:
      - .env
    ports:
      - 3000:3000
    volumes:
      - .:/usr/src/note-api
    command: air main.go -b 0.0.0.0