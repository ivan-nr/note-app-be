services:
  app:
    build: .
    container_name: note-api
    restart: always
    env_file:
      - .env
    ports:
      - 8080:8080
    volumes:
      - .:/usr/src/note-api
    command: air main.go -b 0.0.0.0